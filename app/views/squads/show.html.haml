
%h1=@squad.name

=link_to "edit roster, and add game", edit_squad_path(@squad.id)

%h2="Next Game"
=@squad.games.first.time unless @squad.games.size ==0
%h2="Leader"
=@squad.leader.gamer_tag
%h2="Members"
-@squad.users.each do |u|
  %p=u.gamer_tag
  
=link_to "Add me to this Squad", squad_path(@squad.id, :new_user_id => current_user.id), :method => :put
